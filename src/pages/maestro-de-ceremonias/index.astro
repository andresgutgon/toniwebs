---
import Layout from '@layouts/Default.astro'
import MaestroCeremonias from '@components/MaestroCeremonias'

const props = { locale: 'es', title: 'Maestro de ceremonias' }
---
<Layout
  locale={props.locale}
  title={props.title}
>
  <MaestroCeremonias {...props} />
</Layout>
